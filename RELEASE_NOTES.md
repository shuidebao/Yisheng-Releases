# 译声 YiSheng 1.0.21

本次 1.0.21 正式发布包含新版迷你字幕，以及下方 1.0.20、1.0.19 本地验证阶段积累的韩语与性能优化。下方历史章节保留当时的验证状态；1.0.19、1.0.20 不单独发布，不覆盖任何已有公开版本。

- 迷你字幕采用深色玻璃质感圆角卡片、悬浮胶囊工具条和更清晰的原文/译文层次；重新整理字幕样式面板，控件间距更宽松。
- 默认使用半透明背景，仍支持全透明纯文字；保留用户已设置的字号与颜色，以及锁定、缩放、返回主界面功能。仍只显示当前一句，不恢复四句滚动历史。
- 按实际字体测量适配阅读字号，修正字体单位标示，并为窄窗口和锁定后的解锁按钮预留空间。
- 使用静态绘制，不增加实时屏幕模糊、持续动画或额外翻译模型；不是游戏性能提升承诺。
- 包含此前 1.0.20 的韩语和退出修复；进一步排除开发环境的下次启动清缓存标记，避免分发用户运行状态。

验证：115 项单元测试、轻量 106 通过/9 跳过及前端回归通过。最终安装包 SHA-256、解包 4158 文件逐项比对、四语言离线回归、模型释放和慢请求退出验证通过；最终副本六种原生字幕渲染、正式启动器三轮主窗口/迷你字幕切换和正常退出通过。实际鼠标操作及多 DPI 显示仍待补测，不宣称所有显示缩放均已验证。

# 译声 YiSheng 1.0.20

状态：本地 1.0.20 安装包已生成并完成验收，尚未提交或发布到 GitHub。旧版安装包不含本次功能；此前 1.0.19 的启动器文件消失原因仍待确认，不因新包复测而视为已解决。

- 新增“韩语”原语言和翻译目标，支持韩英互译及经英语中转的韩中、韩日互译；沿用本地 Base 识别，不调用在线翻译 API。
- 自动语言续接支持韩语，韩语长句保留词间空格；迷你字幕按韩文字宽适配字号，仍只保留当前一句。
- 双向韩语模型按需加载，最多两个翻译模型常驻。原中英日模型、参数、两线程与游戏优先调度不变。新增模型约 293.5 MiB 磁盘占用，输出韩语的模型较大，不宣称零额外资源开销。
- 采用官方 OPUS-MT 模型，保留模型包内 CC BY 4.0 许可、来源和转换说明；安装器增加韩语组件完整性校验。
- 修复关闭窗口时后台请求尚未结束、解释器提前关闭线程池而导致清理报错的问题；正常退出等待后台完成，显式限时停止若超时会保留真实状态。慢请求或底层库阻塞仍可能延长退出等待，不声称解决所有退出卡顿。
- 安装包排除构建目录，避免携带旧测试截图和生成的版本源码。

验证范围：108 项单元测试、轻量测试（99 通过/9 跳过）和前端回归通过。最终安装包 SHA-256、实际解包及全部文件比对通过；解包副本中的两条干净真人韩语短录音可识别并译成“谢谢”“谢谢你的帮助”，新增六个韩语相关方向均能离线运行，12 条原英日输出与旧基准一致。实际 WebView、韩语迷你字幕、样式/缩放/锁定/返回与正常退出连续三次通过，启动器及后台均退出 0、文件保留；慢请求退出回归通过。不是游戏准确率或帧率评测。

已知限制：语体可能不一致，中转可能错译或漏译。实测韩语“请稍等”经英语转日语变成了“还有两秒”；一条英译韩长句遗漏了“待在掩体后”。本次完成语言支持，未彻底解决模型语义质量问题。

# 译声 YiSheng 1.0.19

状态：本地更新包已生成，尚未上传 GitHub，暂不建议分发。构建、校验、离线回归和首次桌面启动通过；重复启动验收时解包副本的启动器文件消失，原因待确认，尚未完成最终验收。

2026-09-12 本地更新包新增：减少本地推理完成后的 CPU 空转等待，让计算线程更快把资源让给前台程序。模型、译文参数和线程数不变，尊重已有自定义运行设置；回归输出一致。该项主要降低资源争用，不代表翻译准确率提高，也不承诺固定的游戏帧率或整体提速幅度。

- 迷你字幕恢复为只显示当前原文和译文，移除保留四句、逐句上移的历史队列；主界面字幕历史、字号、颜色、透明度和锁定功能保留。
- 相同原文和翻译方向复用已成功生成的译文，减少重复台词的计算；缓存有容量限制，不写入磁盘，停止同传或清理缓存时释放。
- 小幅缓解部分英译中及日译中多句输入的后半句漏译；单句使用原参数，不增加模型大小、候选数或线程。
- 标准录音直接在内存中处理，减少每段音频的临时文件读写和重复解码；其他音频格式继续使用原解码方式，识别及翻译参数保持不变。
- 增加后端分段耗时信息，便于区分模型加载、音频准备、语音识别和文字翻译，不将后端耗时误当作从讲话到字幕出现的总延迟。

性能边界：本机 5 条干净英语合成音频对照中，标准音频准备由每段约 15–21 毫秒降到约 0.1 毫秒，原文和译文一致；整体处理耗时只变化约 1%，接近运行波动，不代表明显的同传提速或游戏帧率提升。重复译文缓存只加速命中的请求。

已知限制：没有更换识别或翻译模型；游戏俚语、复杂条件句、多人叠音和背景音乐仍可能识别或翻译不准，日语与中文仍经英语中转。此前还观察到一次未定位的退出停滞，后续复测正常，暂不宣称彻底解决。

继续完全离线处理，不需要在线翻译 API。默认 Base、CPU int8、低线程数和游戏优先调度不变。本地验证包不改变线上自动更新入口或历史版本。
