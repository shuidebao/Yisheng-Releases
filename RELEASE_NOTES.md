# 译声 YiSheng 1.0.15

- 改善日语和英语连续语音被切成碎词、短句或无法组成完整句子的问题。
- 日语模式使用更完整的音频窗口、重叠区、候选搜索和滚动上下文，降低助词、句尾及短停顿处的误识别。
- 英语模式使用适中的连续句窗口和上下文，并修复 `interpret-`、`complete---` 等切片边界半词、重复词和尾随连字符。
- 自动语言模式在高置信度连续识别时沿用已检测语言，长停顿后恢复自动检测，减少每个短片段重新猜语言造成的断句。
- 中文继续保留原低延迟参数；所有识别和翻译仍完全离线，不增加付费服务或额外运行时下载。

为换取更完整的连续句，英语首屏通常比旧版增加约一秒延迟，日语也会使用更长的识别窗口。日中互译仍受离线英语中转模型的固有限制，不能保证专业翻译准确率。

公开的旧 Commit、Tag、Release 和历史资产保持不变；本版本继续使用固定 `update.json` 自动更新入口。
