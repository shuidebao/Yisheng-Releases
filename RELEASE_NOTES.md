# 译声 YiSheng 1.0.14

- 修复部分电脑离线翻译偶尔提示 `'NoneType' object has no attribute 'encode'`，导致当前语句无法生成译文的问题。
- 翻译器和两个分词器现在会完整加载后再一次性启用，避免加载中断后遗留半初始化状态。
- 如果发现翻译器或任一分词器状态不完整，下一次翻译会自动卸载并重建模型。
- 非字符串输入和中转翻译的无效结果会被安全拦截，不再向界面泄漏 Python 内部类型错误。

本版本只修复离线翻译状态恢复，不改变识别、字幕、安装目录或自动更新入口。公开的旧 Commit、Tag、Release 和历史资产保持不变。
