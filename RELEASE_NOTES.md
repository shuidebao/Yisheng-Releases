# 译声 YiSheng 1.0.13

- 修复部分电脑首次冷启动时，Python 组件导入、磁盘扫描和 Base 模型预热超过 20 秒，导致应用过早提示“本地同传后端启动超时”的问题。
- 首次启动等待预算扩展到 120 秒；后端线程若确实提前退出，则立即返回真实错误，不会继续等待完整超时时间。
- 日志会记录后端启动预算和实际就绪耗时，便于区分正常的首次预热与真实启动故障。
- 启动失败时先停止本地后端和桌面控制线程，再显示错误对话框，避免模型继续在错误窗口背后加载。

本版本只修复冷启动可靠性，不改变识别、翻译、字幕、安装目录或自动更新功能。公开的旧 Commit、Tag、Release 和历史资产保持不变。
