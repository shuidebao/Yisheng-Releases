# 译声 YiSheng 1.0.17

- 解决“启动译声后游戏变卡”的主要资源竞争：应用打开时不再提前加载 Base 语音模型，只有点击“开始同传”后才准备模型。
- 停止同传并完成最后片段后自动释放识别与翻译模型，减少译声空闲时的内存占用。
- “自动（游戏优先）”模式默认使用低占用 CPU，为游戏保留显卡；需要更快识别时仍可在设置中手动选择 NVIDIA GPU。
- 识别和离线翻译各最多使用 2 个 CPU 线程，Windows 后端使用 BelowNormal 优先级，让前台游戏优先获得处理器调度。
- 修正“清理缓存”统计和反馈：现在会统计临时音频、旧日志、Argos 运行缓存和 WebView 网页缓存，并在清理前释放常驻模型内存。
- 网页缓存会在重启译声时安全删除，界面分别显示“立即释放”和“重启后释放”的空间；遇到被占用文件会提示稍后重试，不再误报全部成功。
- 清理缓存继续保留完整下载的语音模型、离线翻译模型、设置和字幕记录，不会要求重新下载模型。
- 首次点击“开始同传”时可能短暂显示“正在准备本地语音模型”；同传进行中仍需要本机算力，但对游戏 CPU/GPU 的争用已明显降低。

本版本不更换识别或翻译模型，重点改善游戏同时运行时的资源占用和缓存管理可靠性。公开的旧 Commit、Tag、Release 和历史资产保持不变，自动更新继续使用固定 `update.json` 入口。