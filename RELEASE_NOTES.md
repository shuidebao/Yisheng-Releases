# 译声 YiSheng 1.0.12

- 修复英文 Windows 或非 CJK 系统区域设置下，安装接近完成时因中文开始菜单目录无法保存而报 `TargetInvocationException` 的问题。
- 开始菜单快捷方式改用兼容不同系统语言的 `Yisheng` 目录。
- 桌面或开始菜单快捷方式创建失败时，不再把已经完整安装的应用误报为安装失败；安装器会给出可直接启动的文件路径并保存详细日志。
- 安装完成后的自动启动改用经过存在性检查的绝对路径和明确工作目录，启动失败时不再静默忽略。

本版本只修复安装与启动可靠性，不改变识别、翻译、字幕或自动更新功能。公开的 v1.0.11 Tag、Release 和历史资产保持不变。
